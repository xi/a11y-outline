on: push
jobs:
  build:
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - uses: actions/checkout@v3
      - run: make
      - uses: softprops/action-gh-release@v1
        with:
          files: a11y-outline.zip
